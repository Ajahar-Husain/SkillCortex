// Offline knowledge base for the Skill Assistant — used when the Gemini API key
// is missing or rate-limited, so the assistant always answers the actual question.
export default {
  java: 'Java is a statically-typed, object-oriented language that runs on the JVM. Key concepts: OOP pillars (encapsulation, inheritance, polymorphism, abstraction), JVM memory model (heap/stack, garbage collection), collections framework (List, Set, Map), exception handling (checked vs unchecked), multithreading & concurrency (synchronized, ExecutorService), and Java 8+ features (streams, lambdas, Optional). Popular ecosystems: Spring Boot for backends, Android for mobile. Common interview topics: equals/hashCode contract, HashMap internals (buckets + treeification), String immutability, interfaces vs abstract classes.',
  javascript: 'JavaScript is a dynamically-typed, multi-paradigm language that powers the web. Core concepts: event loop & concurrency model (call stack, microtask/macrotask queues), closures, prototypes & prototypal inheritance, `this` binding (call/apply/bind, arrow functions), promises & async/await, ES6+ features (destructuring, spread, modules, optional chaining), and the DOM. Interview favorites: event loop ordering, var/let/const scoping and hoisting, shallow vs deep copy, debounce/throttle implementation.',
  react: 'React is a component-based UI library built around declarative rendering. Key concepts: components & props, state and re-render cycles, hooks (useState, useEffect, useContext, useMemo, useCallback, useRef, useReducer), virtual DOM & reconciliation, keys in lists, controlled vs uncontrolled components, lifting state up vs composition, context for prop-drilling avoidance, performance (React.memo, code splitting, lazy loading), and patterns like custom hooks. React 18+ adds concurrent rendering, automatic batching, and transitions.',
  hooks: 'React Hooks let function components hold state and side effects. **useState** — local state. **useEffect** — side effects (data fetching, subscriptions) with a dependency array controlling when it re-runs; the cleanup function prevents memory leaks. **useContext** — consume context without prop drilling. **useReducer** — complex state logic. **useMemo/useCallback** — memoize values/functions to skip expensive recomputation. **useRef** — mutable value or DOM reference that does not trigger re-renders. Rules: only call hooks at the top level, only in React functions.',
  python: 'Python is a dynamically-typed, interpreted language prized for readability. Core concepts: lists/tuples/dicts/sets and comprehensions, generators & iterators, decorators, context managers (with), the GIL and multiprocessing vs threading, OOP with dunder methods, and typing hints. Ecosystem: Django/Flask/FastAPI for web, NumPy/Pandas for data, PyTorch/TensorFlow for ML. Interview favorites: mutable default-argument pitfall, list vs tuple, shallow vs deep copy, `is` vs `==`, generator memory efficiency.',
  sql: 'SQL is the standard language for relational databases. Core concepts: SELECT/WHERE/GROUP BY/HAVING/ORDER BY, JOINs (inner, left, right, full, self), subqueries & CTEs (WITH), window functions (ROW_NUMBER, RANK, LAG/LEAD), indexes (B-tree, composite, covering), transactions & ACID, normalization (1NF–3NF), and query plans (EXPLAIN). Interview favorites: the N+1 problem, second-highest salary query, deadlock avoidance, and when to denormalize for read performance.',
  mongodb: 'MongoDB is a document-oriented NoSQL database storing flexible JSON-like BSON documents. Key concepts: collections vs tables, embedded documents vs references, indexes, the aggregation pipeline ($match, $group, $lookup, $project), replica sets (high availability) and sharding (horizontal scaling), schema-design patterns (bucket, outlier, extended reference), and multi-document ACID transactions (4.0+). Choose it for flexible/evolving schemas; choose SQL for complex multi-entity joins.',
  node: 'Node.js is a JavaScript runtime built on Chrome V8 with an event-driven, non-blocking I/O model. Key concepts: the event loop (libuv) and its phases, streams & buffers, the module system (CommonJS vs ESM), fs/path/http core modules, clusters & worker_threads for CPU-bound work, Express for routing/middleware, and npm ecosystem management. Interview favorites: how async I/O scales on a single thread, process.nextTick vs setImmediate, backpressure in streams, async error-handling patterns.',
  express: 'Express.js is a minimal, unopinionated web framework for Node.js. Core concepts: routing & route parameters, the middleware pipeline (next(), ordering matters), error-handling middleware (4-arg signature), request/response helpers (res.json, res.status), serving static files, modular routers, body parsing (express.json), CORS configuration, and security basics (helmet, rate limiting, input validation). Production tips: structured logging, graceful shutdown, centralized auth middleware.',
  'next.js': 'Next.js is the leading React meta-framework. Key concepts: file-system routing (app-router layouts, pages, loading/error boundaries), rendering strategies — SSR, SSG, ISR — and server vs client components (the \'use client\' boundary), data fetching with fetch caching, API routes/route handlers, image & font optimization, middleware, and SEO metadata APIs. Interview favorites: when to choose SSR vs SSG vs ISR, server-component benefits, streaming with Suspense.',
  docker: 'Docker packages apps with their dependencies into portable container images. Core concepts: images vs containers, Dockerfile instructions (FROM, COPY, RUN, CMD, ENTRYPOINT, EXPOSE), layer caching, multi-stage builds to shrink image size, volumes vs bind mounts for state, networks, docker-compose for multi-service stacks, and registries. Best practices: small base images (alpine/slim), a .dockerignore, one process per container, run as non-root.',
  aws: 'AWS is the largest cloud platform. Core services for interviews: EC2 (virtual servers, auto-scaling), S3 (object storage, storage classes, presigned URLs), RDS/DynamoDB (managed SQL vs NoSQL), Lambda (serverless, cold starts, concurrency limits), CloudFront (CDN), IAM (users, roles, least privilege), Route 53 (DNS), CloudWatch (monitoring). Architecture favorites: highly-available multi-AZ design, decoupling with SQS/SNS, cost-optimization strategies.',
  'system design': 'System design interviews test architecture reasoning. Framework: 1) clarify requirements & scale (users, QPS, data size), 2) back-of-envelope capacity estimates, 3) API design, 4) data model, 5) high-level components. Building blocks: load balancers, caching (Redis, CDN, cache-aside & invalidation), SQL vs NoSQL trade-offs, replication & sharding, message queues for async decoupling, rate limiting, consistency models (CAP theorem). Classic problems: URL shortener, chat app, news feed, rate limiter.',
  dsa: 'Data structures & algorithms fundamentals: arrays & strings (two pointers, sliding window), hashing (maps/sets for O(1) lookups), linked lists (fast/slow pointers), stacks & queues (monotonic stack), trees (BFS/DFS, BST properties), heaps (top-K problems), graphs (union-find, Dijkstra, topological sort), dynamic programming (memoization → tabulation). Know the Big-O of every operation. Strategy: clarify inputs, trace a small example, state brute force first, then optimize and analyze complexity.',
  git: 'Git is a distributed version control system. Core concepts: the three areas (working directory, staging, repository), commits as snapshots, branches as pointers, merge vs rebase, resolving conflicts, remote workflows (feature branches, pull requests), and undo tools (restore, reset --soft/mixed/hard, revert for shared history). Power tools: stash, cherry-pick, interactive rebase, bisect, reflog for recovery. Conventions: conventional commits, protected main, squash merges.',
  css: 'CSS fundamentals for interviews: the box model, specificity & the cascade (inline > id > class > element), flexbox (main vs cross axis, gap, align/justify), grid (template columns, areas, minmax), positioning (relative/absolute/fixed/sticky), responsive design (media queries, clamp, container queries), and modern features (custom properties, :has()). Tricky favorites: centering techniques, BFC behavior, margin collapse, stacking contexts with z-index.',
  typescript: 'TypeScript adds a static type layer to JavaScript. Core concepts: basic types & inference, interfaces vs type aliases, unions & intersections, narrowing (typeof, in, discriminated unions), generics with constraints, utility types (Partial, Pick, Omit, Record, ReturnType), keyof/typeof operators, unknown vs any (avoid any), strict mode. For React: typing props, children (ReactNode), events, and generic reusable components/hooks.',
  resume: 'Resume tips that pass ATS and impress recruiters: 1) one page per ~8 years of experience, PDF format, single column; 2) lead each bullet with an action verb and quantify impact — "Cut API latency 40% by adding Redis caching" beats "worked on backend"; 3) tailor skills to the job-description keywords (honest ones); 4) structure: contact → summary → experience (reverse-chronological, STAR bullets) → projects with links → skills → education; 5) no photo, tables, or graphics that parsers choke on; 6) proofread — consistent tense and punctuation.',
};